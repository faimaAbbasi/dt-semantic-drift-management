var discovery = require('../../');
var registry = discovery.createRegistry();

setTimeout(function () {
  service.update({
    bar: 'foo'
  });
}, 100);

setTimeout(function () {
  service.update({
    bar: 'foo'
  });
}, 200);

setTimeout(function () {
  service.available = false;
}, 300);

setTimeout(function () {
  process.exit(0);
}, 400);
